#pragma once

namespace Fsnt {
    // Приводит режимы ядра в соответствие с выбранным в простом режиме
    // транспортом.
    //
    // Раньше простой режим умел только включать и выключать TUN, а системный
    // прокси не трогал вовсе. Из-за этого вариант «Системный прокси» был просто
    // выключенным туннелем: в реестре оставался ProxyEnable=0, и весь трафик шёл
    // напрямую мимо VPN, хотя клиент показывал «Подключено».
    // Возвращает, удалось ли привести клиент к выбранному режиму. false —
    // человек отказался выдать права: туннеля не будет, и делать вид, что
    // подключение состоялось, нельзя.
    bool ApplyTransportMode();
} // namespace Fsnt
